# Linux-command-for-window
Implement linux command for window.

```cmd
cmd>> ls                    // "dir" command in window
cmd>> touch text.txt        //  will create text file with name text.txt
```
